process of writing the state of an object to a byte 
to save the staeof you program to  a persistemt storage area such as a file 
at a later time we may restore these objects by using the process of deserialization

class myclass implements Serilization{
    String s;
    int i;
    double d;
    public myclass( string s, int i, Double d){
        this.s = s ;
        this.i = i;
        this.d = d;
    }
    public String toString(){
        retrn "s="+s+"
    }
}\
just we have opened a file that is ready for writing 
but i have connected object output stream to file output stream
oos is a obect created for object output stream
File will accept only bytes
okay
so i am going to supply, oos.write object is obj1 is the obj created to implement serializable interface
what is serealization again: conveting your obj to byte
this method will convert obj to byte
Those byte information is connected to file ouput stream
so the object information is stored into the file
is it clear
is it clear
go to the next line 
just check the red highlited oned 
File information is used to supply the byte details
object input stream is a channel that recieves the information 
WriteOBject is used to retrive the information of the class
and i am trying to convert from byte to obj
Then write object is more than enough
connetction may be terminated
available is used to check if the information is available in the buffer
it is not a derevative of input output streams 
here they have used name of the file 
they have opened a file in read and write mode
 what are all the methods that re used
 theya are using different functions to write different methods
 they are trying to set a file pointer to a location 0
 they are trying ot set the pointer to the 0th location 
 you can read the information using read 
 that has been stored in different variables
 random access variable, here they are trying to point to a location 
 what are the object state that are available the obj state has to be given witht the obj
 in the slides they have given some problems that also has to be implemented
 